# Release Notes - v3.0.6

## Improvements
- **Verified Formatting**: Applied and verified `rustfmt` compliance locally using the latest stable toolchain to prevent CI regression.
- **Robust CI Pipeline**: Refined integration tests to be more resilient to environment differences.

## Bug Fixes
- Fixed minor whitespace and comment formatting issues in `src/configs.rs` that were flagged by GitHub Actions.
