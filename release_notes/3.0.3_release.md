# Release Notes - v3.0.3

## Improvements
- **Precision Formatting**: Manually aligned the codebase with `rustfmt` rules as strictly as possible to fix CI failures.
- **Dependency Update**: Updated internal lockfile.

## Bug Fixes
- Fixed minor formatting issues in `src/configs.rs` and `src/main.rs` that were causing the integration test workflow to fail.
